# 番茄温室 RGB-D 数据集质量分析评估报告

**项目**：番茄采摘机器人 VLA 仿真环境构建
**评估对象**：现有 4 组 RGB-D 数据集（L515/D435 × 白天/夜间，各约 122 帧，1280×720）
**评估目的**：判定现有数据能否支撑 real2sim 重建（COLMAP/TSDF/八叉树/3DGS），定位各组数据的缺陷根源，给出补采规格
**评估方式**：对每组数据完整执行对应的重建管线实验，以可复现的量化指标为准（全部实验脚本在 `scripts/`，日志在 `logs/`）
**日期**：2025-09-03

---

## 一、评估结论（TL;DR）

1. **现有 4 组数据集均无法单独支撑"全局、米制精确、可交互"的仿真环境重建**；
2. **L515 夜间是唯一重建成功的数据源**（纯深度 ICP 路线），已产出 7 段局部地图与 1 个精修背景资产（漂移 10.6cm），但受单侧视角/无回环/活动光照限制，定位为"布局参考 + 静态背景片"；
3. 失败根源不在重建算法（多路线交叉验证），而在**采集方式未满足重建的三条输入前提**：全局一致的位姿尺度、大面积有效深度、视角重叠与回环；
4. 补采一批符合规格的数据（规格见第六节）后，现有全部管线无需修改即可直接产出目标场景。

---

## 二、数据集概况与单集评估

### 通用信息

- 传感器：Intel RealSense L515（558nm MEMS 激光雷达）与 D435（860nm 红外双目结构光），robot 轨道底盘搭载，eye-to-hand 安装
- 数据规格：1280×720 彩色 PNG + 16bit 深度 PNG 成对保存；L515 深度单位 0.25mm/LSB（官方 `拍摄相机内参.txt`），D435 数据集深度单位 0.1mm/LSB（经 uint16 饱和截断检验确认）
- 内参：L515 fx=904.136/fy=904.541（官方 1280×720 导出，经 COLMAP 0.5px 重投影误差实战验证）；畸变 Brown-Conrady [0.171, -0.526, ...]（历史实验用 PINHOLE 忽略，边缘存在少量偏差）
- 采集方式：机器人**运动中**周期存帧（帧间隔中位 5s，存在停顿与 55s 级大间隔），**无位姿/里程计记录，无回环设计，无静止停点**

### 2.1 L515 夜间（122 帧）

| 指标 | 实测值 |
|---|---|
| 深度有效率 | 37%~71%，中位 54%，122 帧全部 ≥37%（无坏帧） |
| 场景深度 | 中位 ~1.7m（0.4~5m 有效范围） |
| 彩色 | 补光灯照明，亮度随帧波动 54~70，纹理弱 |
| 帧间重叠 | 相邻帧特征匹配充足（ORB 200/200 满配） |

**问题**：
1. 彩色不可用于 SfM——低纹理 + 补光灯随视角移动（光照视角相关），COLMAP 仅 3~4/60 帧注册；
2. 补光活动光源违反 3DGS/光度重建的静态光照假设；
3. 采集无回环，帧 41~47 存在场景突变（运动加速 3 倍 + 外观从暗疏区变亮密区）。

**实验与结果**：
| 实验 | 结果 |
|---|---|
| COLMAP 纯视觉（放宽初始化+顺序匹配+1.2万特征/图） | ❌ 4/60 注册 |
| 纯深度 ICP 配准（点到点0.4m粗配→点到面0.2m精配） | ✅ fitness 0.71~0.92，帧间平移 4~16cm、旋转 0~5.6° |
| 40 帧链式配准+位姿图（相邻边+隔10帧回环边） | ✅ 86.9万点/118万面，漂移 10~50cm；投影回原图验证对齐（鬼影率 21~31%，遮挡 34~52%） |
| 80 帧链式 | ⚠️ 帧 41 后漂移 235cm/36.5°（场景突变段），证实必须分段 |
| 全量 122 帧自动分段建图 | ✅ 7 段（帧 0-13/16-26/27-40/48-60/68-79/80-86/87-121），突变段自动隔离 |
| 7 段量化评比 + 最优段精修 | ✅ 选出 seg_01（16-26帧，漂移 10.6cm），Gamma 提亮后输出 `recon/final_env/` |
| 3DGS 重训（8 帧 + ICP 位姿 + 官方内参） | ⚠️ 训练 PSNR 15.9，**新视角 PSNR 8.8**——帧数不足+活动光照卡死新视角质量 |

**结论**：唯一重建成功的数据源。产出：7 段局部地图 + 精修背景资产 + 定量质量基线。上限：布局参考/静态背景片。

### 2.2 L515 白天（122 帧）

| 指标 | 实测值 |
|---|---|
| 深度有效率 | **4~15%**（逐帧扫描） |
| 彩色 | 四组中最好（自然光、纹理丰富） |
| 采集连续性 | 存在停顿；COLMAP 注册断成 **5 个孤立模型**（44+39+25+27+10 帧） |

**问题（物理性根源）**：
1. L515 工作波长 558nm 处于太阳光谱能量峰值，且叶片强反射 550nm 附近光——**日光+叶面反射淹没接收器**，深度系统性失效（该传感器本质为室内设计）；
2. 彩色虽好，但 COLMAP 平移尺度漂移 **2.5~6.3 倍且随空间位置变化**（重复纹理的成排相似盆栽 + 窄基线；若为全局统一比例尚可用深度一次性校正，实测比值 p10=1.18/中位=1.46/p90=2.59 证明非均匀）；
3. 深度无效导致无米制锚点，尺度漂移无法校正；
4. 采集不连续 → 5 个模型之间无几何桥梁。

**实验与结果**：
| 实验 | 结果 |
|---|---|
| 帧间重叠检验（ORB） | ✅ 相邻帧 200/200 满配 |
| COLMAP 增量重建（40/122 帧） | ✅ 30~44 帧注册，重投影误差 0.5px（旋转可靠） |
| 深度反投影 + TSDF 融合 | ⚠️ 300 万点 → mesh 2.9 万面，但鬼影/碎片严重 |
| Poisson 密面重建 | ❌ 薄叶片被"充气"成鼓包（用户反馈"糊成一团"的元凶），弃用 |
| 4mm 细体素 TSDF + 最佳视角取色 | ⚠️ 结构改善，但受限于深度稀疏 |
| 尺度诊断（同帧 COLMAP深度/实测深度比值） | ❌ 证实非均匀尺度漂移，死刑判决 |
| 替代位姿方案×3（RGBD里程计/特征配准位姿图/混合定姿） | ❌ 全部失败（详见 2.3 同类实验） |

**结论**：彩色可支撑位姿旋转估计（已验证），深度物理性失效且无法补救。产出：44 帧旋转位姿 + "尺度漂移"定量结论。该数据集的**正确用途**是补采时作为"白天彩色质量"的基准参照。

### 2.3 D435 白天（120 帧）

| 指标 | 实测值 |
|---|---|
| 深度有效率 | ~30%（含 0.3~3m 截断） |
| 场景深度 | 中位 0.64m（近景） |
| 视场 | 69°×42°，每帧覆盖约一株冠层 |
| 彩色 | 好 |

**问题**：
1. 深色叶片红外纹理弱 → 立体匹配失败；近距拍摄低于理想 min-z；二者叠加致 70% 深度缺失；
2. **近景+窄视场**：帧间配准可以，但视场并集天然碎片化，无法拼成连续场景；
3. 附带发现：D435i 内参 JSON 与数据集不符（640×480 会话、深度单位 1mm vs 数据集 0.1mm），需重导。

**实验与结果**：
| 实验 | 结果 |
|---|---|
| FPFH 特征 RANSAC + PointToPlane ICP + 位姿图优化 + TSDF 全管线 | ⚠️ 配准 fitness **0.76~0.99**（四组最高），但 mesh 依然碎片化 |
| 深度单位判别（饱和截断检验：0.02% 像素=65535 → 0.1mm 单位下 6.55m 合理 / 1mm 下 65.5m 不可能） | ✅ 确认数据集深度单位 0.0001 |

**结论**：**配准质量最好的数据集，但采集视场决定其只能产出局部碎片**。该数据集的深度质量问题在补采时最易解决（拉开距离 1~2m、放缓重叠）。

### 2.4 D435 夜间（119 帧）

| 指标 | 实测值 |
|---|---|
| 深度有效率 | 极低 |
| 彩色 | 差（补光不足） |

**实验与结果**：仅做质量初筛，确认不可用后未投入进一步实验。

**结论**：弃用。

---

## 三、横向对比与根因分析

### 3.1 四组数据横向对比

| | L515 夜 | L515 昼 | D435 昼 | D435 夜 |
|---|---|---|---|---|
| 深度有效率 | **37~71%** | 4~15% | ~30% | 极低 |
| 彩色/配准 | 弱（3/60注册） | **好**（0.5px） | 好（fitness 0.99） | 差 |
| 度量尺度 | ✅ 深度ICP可锚定 | ❌ 漂移2.5~6.3× | ✅ 配准质量高 | — |
| 视野覆盖 | 单侧幕帘 | 断成5段 | 单株碎片 | — |
| **重建产出** | **7段局部地图+背景资产** | 旋转位姿 | 配准结论 | 无 |
| 卡点 | 无回环+活动光照 | 深度物理失效 | 近景窄视场 | 深度失效 |

### 3.2 根因：三条重建前提与采集方式的矛盾

| 重建前提 | 数据现实 | 后果 |
|---|---|---|
| 全局一致位姿尺度 | SfM 在重复纹理+窄基线下尺度非均匀漂移；无里程计记录可锚定 | 任何深度融合必然鬼影（实验验证于 L515昼/D435昼） |
| 大面积有效深度 | L515昼被日光物理淹没；D435 近景+叶面弱纹理 | 每帧只贡献孤立小片（L515昼）/ 单株碎片（D435） |
| 重叠+回环 | 运动中周期存帧、无停点环绕、无回环设计、存在场景突变 | 链式漂移不可控（ICP 60帧±400m 实测）；3DGS 帧数不足（新视角 PSNR 8.8） |

**核心判断**：表征（mesh/八叉树/3DGS）不是瓶颈——同一批输入在三种表征下都得到同级别的碎片化输出；**瓶颈是数据采集与重建前提的错配**。

### 3.3 交叉验证记录（排除算法因素）

为排除"实现不当"的可能，同一数据上交叉尝试了多类位姿来源与融合方法：
- 位姿来源：COLMAP SfM（2种匹配策略×2组放宽参数）、RGBD稠密里程计、FPFH特征配准+位姿图全局优化、混合定姿（COLMAP旋转+深度ICP平移）、轨道约束讨论；
- 融合方法：TSDF（8/6/4mm 体素）、Poisson、点云累积+体素下采样、八叉树方案论证；
- 配准环节均达到可用质量（fitness 0.4~0.99），失败均发生在**全局一致性与覆盖**层面——即输入数据层面。

---

## 四、3DGS 专项评估（补充章节）

3DGS 管线已完整自研实现（`scripts/train_3dgs_v2.py`，gsplat CUDA 扩展编译通过，环境持久化于 `~/tomato_robot/cuda_toolkit`）。

| 版本 | 数据 | 位姿 | 内参 | 训练PSNR | 新视角PSNR |
|---|---|---|---|---|---|
| v1 | 3帧夜景 | COLMAP（任意尺度） | fx=614.87（COLMAP自由估计，**错误**） | 17.9 | 未评估 |
| v2 | 8帧夜景（剔除3坏帧） | 深度ICP位姿图（漂移10.6cm） | **fx=904.136 官方值** | 15.9 | **8.8** |

**结论**：v2 修正了 v1 的尺度冲突（米制深度点云 vs 任意尺度位姿混用）与内参错误，训练视角渲染色调/结构正确，但新视角 PSNR 8.8 不可用。根因：**7 帧视角冗余不足 + 活动光照违反静态外观假设**。训练视角下的针刺状伪影（拉长高斯）是帧数不足的典型特征。管线就绪，等补采数据。

---

## 五、现有可用资产清单

| 资产 | 位置 | 状态 |
|---|---|---|
| 精修背景 mesh（55.8万顶点/69.3万面，提亮） | `recon/final_env/sim_environment_mesh.ply` | ✅ 可用（布局/背景层） |
| 带色点云（20万点） | `recon/final_env/sim_environment.ply` | ✅ 可用 |
| 7 段局部地图（每段 ply/mesh/位姿/帧清单） | `recon/night_map/seg_00~06/` | ✅ 可用（布局参考） |
| L515 白天 44 帧旋转位姿 | `recon/l515day_full/sparse/` | ⚠️ 旋转可靠，平移尺度漂移 |
| 3DGS v2 管线 + 模型（新视角不可用） | `scripts/train_3dgs_v2.py` / `output/3dgs_night_v2/` | 管线就绪 |
| 投影一致性验证图 | `recon/previews/overlay_frame*.png` | 验收证据 |

---

## 六、补采数据规格（使现有管线直接产出目标场景）

按解决三个根因设计，优先级排序：

### 必须项
1. **停点环绕采集**：每个停点绕目标区域（单株/单段垄道）环绕拍摄 30~60 张，相邻视角重叠 ≥70%，每个停点内部构成天然回环；
2. **走走停停**：停点间距 0.5~1m，相邻停点视野重叠 ≥50%（段间桥梁）；
3. **记录里程**：底盘轮式里程计/RTK 任一，精度 ±10cm 即可（提供尺度锚与初值）。

### 参数项
4. **相机**：D435 优先（避开 L515 日光问题），距离目标 1~2m（min-z 以上），固定曝光/白平衡（夜间补光则固定灯位相对位姿）；
5. **内参与深度**：按 `拍摄相机内参.txt` 同方式导出会话内参（1280×720、深度单位固定并在文档注明）；D435 深度分辨率建议 848×480@30fps 提升近距离质量；
6. **光照一致性**：白天拍摄或人工固定照明（灯不随机位移动），避免正视强反光面。

### 量目标
7. 单个"可重建区域"（约 3m 垄道）：≥80 帧、重叠连续、深度有效率 ≥70%；
8. 交付物：RGB/深度/内参 JSON/里程计 CSV 各一套 + 采集记录表。

**预期**：满足上述规格的数据，经现有管线（`night_depth_map.py`/`rgbd_final_recon.py`/`train_3dgs_v2.py`）无需算法修改即可产出：全局米制精确的点云/mesh（用于背景与碰撞）+ 可用新视角的 3DGS（用于视觉增强），预计新视角 PSNR 可达 25dB+ 区间（参照正常多视角采集的经验水平）。

---

## 七、附：实验索引

| 实验 | 脚本 | 日志/产物 |
|---|---|---|
| ManiSkill 环境验证 | `scripts/verify_maniskill.py` | `logs/` |
| 夜间单段建图 | `scripts/night_depth_map.py` | `logs/night_map*.log` |
| 全量分段建图 | `scripts/night_segment_map.py` | `logs/seg_map.log` |
| 白天 COLMAP+TSDF | `scripts/archive/reconstruct_day.py` | `logs/recon_full.log` |
| D435 特征配准管线 | `scripts/archive/rgbd_final_recon.py` | `logs/final_recon*.log` |
| 3DGS v2 训练 | `scripts/train_3dgs_v2.py` | `logs/3dgs_v2.log` |
| 内参/深度单位验证 | `scripts/archive/` 及各诊断脚本 | 本报告 2.2/2.3 节 |
